<form method="post" action="{{ path('app_usertable33_delete', {'id': usertable33.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usertable33.id) }}">
    <button class="btn">Delete</button>
</form>
