{% extends 'base.html.twig' %}

{% block title %}FORM{% endblock %}

{% block body %}
    <h1>Create-Data</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usertable34.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ usertable34.Username }}</td>
            </tr>
            <tr>
                <th>Useremailid</th>
                <td>{{ usertable34.Useremailid }}</td>
            </tr>
            <tr>
                <th>Userphonenumber</th>
                <td>{{ usertable34.Userphonenumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_usertable34_index') }}">back to list</a>

    <a href="{{ path('app_usertable34_edit', {'id': usertable34.id}) }}">edit</a>

    {{ include('usertable34/_delete_form.html.twig') }}
{% endblock %}
